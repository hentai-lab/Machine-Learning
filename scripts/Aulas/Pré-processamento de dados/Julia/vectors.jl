# VECTORS
# Um conjunto de valores da mesma classe.

pesos = [7.76, 80.3, 90, 117.4] # Cria um vetor dinâmico com tamanho variado
print(pesos) # Printa os valores do vetor no console

nomes = ["Maria", "Carlos", "Pedro"] # Cria um vetor dinâmico com tamanho variado
print(nomes) # Printa os valores do vetor no console